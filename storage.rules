rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read on uploaded assets, and allow uploads for layers/backgrounds
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
